name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: generate
      run: cmake .
    - name: build
      run: cmake --build .
    - name: test
      run: ctest
    
